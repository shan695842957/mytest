-- ============================================================================
-- 迁移版本: v1.4.0
-- 创建时间: 2025-01-XX
-- 说明: 为 device_type_tags 表添加缺失的 enum_json 列
-- ============================================================================

-- 检查并添加 enum_json 列（如果不存在）
-- SQLite 不支持直接检查列是否存在，所以使用 ALTER TABLE ADD COLUMN
-- 如果列已存在，会报错，但我们可以忽略（在生产环境中需要更谨慎）

-- 为 device_type_tags 表添加 enum_json 列
ALTER TABLE device_type_tags 
ADD COLUMN enum_json TEXT NOT NULL DEFAULT '{}';

-- 注意：如果列已存在，上面的语句会失败，这是正常的
-- 在生产环境中，应该先检查列是否存在，但 SQLite 不支持这种检查
-- 如果执行失败，说明列已存在，可以忽略错误

